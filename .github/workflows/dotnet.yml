# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: .NET

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build-android:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 10.0.x
    - name: Install MAUI Android workload
      run: dotnet workload install maui-android
    - name: Restore dependencies
      run: dotnet restore ASecureNoteMaker/ASecureNoteMaker.csproj -p:TargetFramework=net10.0-android
    - name: Build Android
      run: dotnet build ASecureNoteMaker/ASecureNoteMaker.csproj -p:TargetFramework=net10.0-android --no-restore

  build-windows:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v4
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 10.0.x
    - name: Install MAUI Windows workload
      run: dotnet workload install maui
    - name: Restore dependencies
      run: dotnet restore ASecureNoteMaker/ASecureNoteMaker.csproj -p:TargetFramework=net10.0-windows10.0.19041.0
    - name: Build Windows
      run: dotnet build ASecureNoteMaker/ASecureNoteMaker.csproj -p:TargetFramework=net10.0-windows10.0.19041.0 --no-restore

  # iOS build requires Xcode 26.3 but GitHub runners only have 16.4 — re-enable when runners are updated
  # build-ios:
  #   runs-on: macos-latest
